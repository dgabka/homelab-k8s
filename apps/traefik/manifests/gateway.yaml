apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: main
  namespace: traefik
spec:
  gatewayClassName: traefik
  listeners:
    - name: http
      protocol: HTTP
      port: 8800
      allowedRoutes:
        namespaces:
          from: All # HTTPRoutes from any namespace can attach
